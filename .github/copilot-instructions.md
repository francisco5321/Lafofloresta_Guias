# Copilot Instructions

- This workspace targets a Windows desktop .NET solution for wooden guias.
- Keep the PDF generator rules exact: A5, 4 pages, white background, page-specific header colors, barcode only on via 1, and the stamp only on vias 2 to 4.
- Prefer minimal, focused changes that preserve the existing structure.
- Keep SQLite schema names aligned with `proprietarios`, `destinatarios`, `rolaria`, and `guias`.
