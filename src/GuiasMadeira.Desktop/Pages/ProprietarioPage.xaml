<Page x:Class="GuiasMadeira.Desktop.Pages.ProprietarioPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Proprietários">
    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="Dados" Style="{StaticResource BreadcrumbLink}"/>
            <Path Style="{StaticResource BreadcrumbSeparator}"/>
            <TextBlock Text="Proprietários" Style="{StaticResource BreadcrumbLink}"/>
            <Path Style="{StaticResource BreadcrumbSeparator}"/>
            <TextBlock x:Name="BreadcrumbCurrentText" Text="Novo proprietário" Style="{StaticResource BreadcrumbCurrent}"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="440"/>
                <ColumnDefinition Width="24"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto" VerticalAlignment="Top">
                <Border Style="{StaticResource Card}" Width="Auto" Height="Auto" HorizontalAlignment="Stretch">
                    <StackPanel>

                        <Border x:Name="ToastBorder" Style="{StaticResource Toast}">
                            <StackPanel Orientation="Horizontal">
                                <Path Data="{StaticResource IconCheck}" Stroke="{StaticResource AccentBrush}" StrokeThickness="2.2"
                                      StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round"
                                      Width="14" Height="14" Stretch="Uniform" Margin="0,0,8,0"/>
                                <TextBlock x:Name="ToastText" Text="Proprietário guardado" Foreground="{StaticResource AccentPressedBrush}" FontWeight="SemiBold" FontSize="13"/>
                            </StackPanel>
                        </Border>

                        <TextBlock x:Name="FormTitleText" Text="Novo proprietário" Style="{StaticResource PageTitle}"/>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                            <Path Style="{StaticResource FieldIcon}" Data="{StaticResource IconTree}"/>
                            <TextBlock Text="Nome" Style="{StaticResource FieldLabel}" Margin="0"/>
                        </StackPanel>
                        <TextBox x:Name="NomeBox"/>
                        <TextBlock x:Name="NomeError" Style="{StaticResource InlineError}" Margin="0,-12,0,16"/>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                            <Path Style="{StaticResource FieldIcon}" Data="{StaticResource IconPin}"/>
                            <TextBlock Text="Distrito" Style="{StaticResource FieldLabel}" Margin="0"/>
                        </StackPanel>
                        <TextBox x:Name="DistritoBox"/>
                        <TextBlock x:Name="DistritoError" Style="{StaticResource InlineError}" Margin="0,-12,0,16"/>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                            <Path Style="{StaticResource FieldIcon}" Data="{StaticResource IconPin}"/>
                            <TextBlock Text="Concelho" Style="{StaticResource FieldLabel}" Margin="0"/>
                        </StackPanel>
                        <TextBox x:Name="ConcelhoBox"/>
                        <TextBlock x:Name="ConcelhoError" Style="{StaticResource InlineError}" Margin="0,-12,0,16"/>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                            <Path Style="{StaticResource FieldIcon}" Data="{StaticResource IconPin}"/>
                            <TextBlock Text="Freguesia" Style="{StaticResource FieldLabel}" Margin="0"/>
                        </StackPanel>
                        <TextBox x:Name="FreguesiaBox"/>
                        <TextBlock x:Name="FreguesiaError" Style="{StaticResource InlineError}" Margin="0,-12,0,16"/>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                            <Path Style="{StaticResource FieldIcon}" Data="{StaticResource IconTag}"/>
                            <TextBlock Text="Código de propriedade (opcional)" Style="{StaticResource FieldLabel}" Margin="0"/>
                        </StackPanel>
                        <TextBox x:Name="CodigoPropBox"/>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                            <Path Style="{StaticResource FieldIcon}" Data="{StaticResource IconTag}"/>
                            <TextBlock Text="Parcela (opcional)" Style="{StaticResource FieldLabel}" Margin="0"/>
                        </StackPanel>
                        <TextBox x:Name="ParcelaBox"/>

                        <StackPanel Orientation="Horizontal" Margin="0,8,0,0" HorizontalAlignment="Center">
                            <Button x:Name="GuardarButton" Content="Guardar" Style="{StaticResource PrimaryButton}" Click="Guardar_Click"/>
                            <Button x:Name="CancelarEdicaoButton" Content="Cancelar edição" Style="{StaticResource SecondaryButton}"
                                    Margin="10,0,0,0" Visibility="Collapsed" Click="CancelarEdicao_Click"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </ScrollViewer>

            <Border Grid.Column="2" Style="{StaticResource Card}" Width="Auto" Height="Auto"
                    HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Proprietários registados" Style="{StaticResource PageTitle}" FontSize="18" Margin="0,0,0,14"/>

                    <StackPanel Grid.Row="1" Margin="0,0,0,4">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                            <Path Style="{StaticResource FieldIconStroke}" Data="{StaticResource IconSearch}"/>
                            <TextBlock Text="Pesquisar" Style="{StaticResource FieldLabel}" Margin="0"/>
                        </StackPanel>
                        <TextBox x:Name="PesquisaBox" Margin="0,0,0,12" TextChanged="Pesquisa_TextChanged"/>
                    </StackPanel>

                    <Border x:Name="ListaEmptyState" Grid.Row="2" Style="{StaticResource EmptyState}" VerticalAlignment="Top">
                        <StackPanel HorizontalAlignment="Center">
                            <Path Data="{StaticResource IconEmptyBox}" Stroke="{StaticResource TextMutedBrush}" StrokeThickness="1.6"
                                  StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round"
                                  Width="28" Height="28" Stretch="Uniform" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                            <TextBlock Text="Ainda não há proprietários registados." Style="{StaticResource HelperText}" TextAlignment="Center"/>
                        </StackPanel>
                    </Border>

                    <DataGrid x:Name="ListaGrid" Grid.Row="2" AutoGenerateColumns="False" IsReadOnly="True"
                              CanUserAddRows="False" CanUserDeleteRows="False" CanUserReorderColumns="False"
                              GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="{StaticResource BorderBrush}"
                              RowBackground="{StaticResource SurfaceBrush}" AlternatingRowBackground="{StaticResource BackgroundBrush}"
                              BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                              ColumnHeaderStyle="{StaticResource ListGridHeader}" CellStyle="{StaticResource ListGridCell}"
                              HeadersVisibility="Column" RowHeaderWidth="0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Nome" Binding="{Binding Nome}" Width="2*"/>
                            <DataGridTextColumn Header="Distrito" Binding="{Binding Distrito}" Width="*"/>
                            <DataGridTextColumn Header="Concelho" Binding="{Binding Concelho}" Width="*"/>
                            <DataGridTextColumn Header="Freguesia" Binding="{Binding Freguesia}" Width="*"/>
                            <DataGridTemplateColumn Header="" Width="80">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Style="{StaticResource GhostIconButton}" ToolTip="Editar" Click="Editar_Click">
                                                <Path Data="{StaticResource IconEdit}" Fill="{StaticResource TextMutedBrush}"
                                                      Width="15" Height="15" Stretch="Uniform"/>
                                            </Button>
                                            <Button Style="{StaticResource GhostIconButton}" ToolTip="Apagar" Click="Apagar_Click">
                                                <Path Data="{StaticResource IconTrash}" Fill="{StaticResource ErrorBrush}"
                                                      Width="15" Height="15" Stretch="Uniform"/>
                                            </Button>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Page>
